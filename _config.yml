# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
baseurl: "" # leave empty for a user site at xiyuez2.github.io
url: "https://xiyuez2.github.io" # absolute URLs for the sitemap, canonical links and social cards

# Build settings
markdown: kramdown
plugins:
  - jekyll-email-protect
  - jekyll-sitemap

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
# exclude:
#   - Gemfile
#   - Gemfile.lock
#   - node_modules
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/

# timezone: Asia/Shanghai

collections:
  - publications
  - news
  - showcase

# Setting `exclude` replaces Jekyll's defaults, so they are listed again here.
# vendor/ matters on CI, where bundler installs gems into vendor/bundle.
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/
  - xiyue-website-debug   # nested clone of the old debug site; not part of this site
